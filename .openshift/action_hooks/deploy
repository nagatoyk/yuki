#!/bin/bash


if [ ! -d $OPENSHIFT_DATA_DIR/uploads ]; then
    mkdir $OPENSHIFT_DATA_DIR/uploads
fi
cp -rf $OPENSHIFT_REPO_DIR/uploads/* $OPENSHIFT_DATA_DIR/uploads/ 2>/dev/null
ln -sf $OPENSHIFT_DATA_DIR/uploads   $OPENSHIFT_REPO_DIR